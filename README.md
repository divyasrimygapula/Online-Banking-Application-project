# Online-Banking-Application-project
Automated an Online Banking application using Selenium WebDriver with Java and TestNG. Implemented Page Object Model (POM) to automate core features like login, balance enquiry, and fund transfer. Maven is used for dependency management and tests are executed through TestNG on the ParaBank demo application.
